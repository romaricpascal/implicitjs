const {basename, extname} = require('path');

module.exports = function(path) {
  return withoutExtension(withoutExtension(path));
};

function withoutExtension(path) {
  return basename(path, extname(path));
}